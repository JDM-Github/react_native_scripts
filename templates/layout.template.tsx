/*
import { Text, View } from 'react-native';
import { __NAME__Constant } from '@/core/constants/layouts/__KEBAB_NAME__.layout.constant';
import type { __NAME__LayoutController } from '@/core/controllers/layouts/__KEBAB_NAME__.layout.controller';
import { useNotifier } from '@/core/foundation/use-notifier';

export interface __NAME__LayoutProps {

  controller: __NAME__LayoutController;

}


export function __NAME__Layout({ controller }: __NAME__LayoutProps) {
  useNotifier(controller);
  return (
    <View className="flex-1 bg-canvas p-4">
      <Text className="text-2xl font-bold text-ink">{__NAME__Constant.title}</Text>
      <Text className="mt-1 text-sm text-ink-muted">{controller.selectedMode}</Text>
    </View>
  );
}
*/
