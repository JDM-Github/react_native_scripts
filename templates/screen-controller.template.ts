/*
import { __NAME__Mode } from '@/core/enums/screens/__KEBAB_NAME__.screen.enum';
import { Notifier } from '@/core/foundation/notifier';

export class __NAME__ScreenController extends Notifier {

  private mode: __NAME__Mode = __NAME__Mode.default;

  get selectedMode(): __NAME__Mode {
    return this.mode;
  }

  changeMode = (mode: __NAME__Mode): void => {
    if (this.mode === mode) return;
    this.mode = mode;
    this.notifyListeners();
  };

}
*/
