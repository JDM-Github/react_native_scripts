/*
import { Text, View } from 'react-native';
import { __NAME__Constant } from '@/core/constants/screens/__KEBAB_NAME__.screen.constant';
import type { __NAME__ScreenController } from '@/core/controllers/screens/__KEBAB_NAME__.screen.controller';
import { useNotifier } from '@/core/foundation/use-notifier';

export interface __NAME__ScreenProps {

  controller: __NAME__ScreenController;

}


export function __NAME__Screen({ controller }: __NAME__ScreenProps) {
  useNotifier(controller);
  return (
    <View className="flex-1 bg-canvas p-4">
      <Text className="text-2xl font-bold text-ink">{__NAME__Constant.title}</Text>
      <Text className="mt-1 text-sm text-ink-muted">{controller.selectedMode}</Text>
    </View>
  );
}
*/
